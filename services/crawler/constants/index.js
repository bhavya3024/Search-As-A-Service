const paginationType = {
    OFFSET: 'OFFSET',
    INCREMENT: 'INCREMENT',
}

const requestParameter = {
    BODY: 'body',
    HEADERS: 'headers'
}

const security = {
    PUBLIC: 'PUBLIC',
    SECURE: 'SECURE',
}

module.exports = {
    paginationType,
    requestParameter,
    security,
}
